<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>إهداء</title>

<style>
    @import url('https://fonts.googleapis.com/css2?family=Amiri:wght@700&family=Tajawal:wght@300;400&display=swap');

    body {
        margin: 0;
        padding: 0;
        height: 100vh;
        background: linear-gradient(to bottom, #0c0f15 0%, #161c24 100%);
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        overflow: hidden;
        color: #f4eee2;
        font-family: 'Tajawal', sans-serif;
    }

    h1 {
        font-family: 'Amiri', serif;
        font-size: 3rem;
        margin: 0 0 10px;
        text-shadow: 0 0 20px rgba(255,150,150,0.4), 0 0 10px rgba(255,70,70,0.2);
    }

    p {
        font-size: 1.4rem;
        margin: 0;
        opacity: 0.85;
        letter-spacing: 0.4px;
    }

    /* حاوية الورد */
    .flower-box {
        width: 360px;
        height: 360px;
        margin-top: 25px;
        position: relative;
        filter: drop-shadow(0 0 18px rgba(255, 200, 200, 0.25));
    }

    /* طبقة التوهج */
    .glow {
        position: absolute;
        width: 100%;
        height: 100%;
        top: 0; left: 0;
        background: radial-gradient(circle, rgba(255,200,200,0.15) 0%, transparent 70%);
        filter: blur(25px);
        animation: glowPulse 5s ease-in-out infinite;
    }

    @keyframes glowPulse {
        0%, 100% { opacity: 0.4; }
        50% { opacity: 0.75; }
    }

    svg {
        width: 100%;
        height: 100%;
    }

    .stem {
        stroke: #4aa06e;
        stroke-width: 4;
        fill: none;
        filter: drop-shadow(0 0 6px rgba(0,255,170,0.2));
    }

    .leaf {
        fill: #47a371;
        filter: brightness(1.08);
        opacity: 0.9;
    }

    .petal {
        fill: #ffd7c7;
        filter: drop-shadow(0 0 10px rgba(255,210,210,0.4));
    }

    .center {
        fill: #ffe28a;
        filter: drop-shadow(0 0 6px rgba(255,210,120,0.5));
    }

    /* حركة التمايل */
    .sway {
        transform-origin: bottom center;
        animation: swayAnim 5s ease-in-out infinite;
    }

    @keyframes swayAnim {
        0%, 100% { transform: rotate(-2deg); }
        50% { transform: rotate(3deg); }
    }

</style>
</head>

<body>

<h1>حبيبي 🥺❤️❤️</h1>
<p>رضاك أزعل الدنيا عشان أرضيه</p>

<div class="flower-box">
    <div class="glow"></div>

    <svg viewBox="0 0 200 200">
        <g class="sway">
            <path class="stem" d="M100 180 Q 102 140, 100 100" />
            <path class="leaf" d="M100 135 Q 85 130, 70 115 Q 90 120, 100 135 Z" />
            <path class="leaf" d="M100 120 Q 115 110, 130 95 Q 110 100, 100 120 Z" />

            <g transform="translate(100, 100)">
                <circle class="petal" cx="0" cy="-17" r="13" />
                <circle class="petal" cx="15" cy="-5" r="13" />
                <circle class="petal" cx="10" cy="13" r="13" />
                <circle class="petal" cx="-10" cy="13" r="13" />
                <circle class="petal" cx="-15" cy="-5" r="13" />
                <circle class="center" cx="0" cy="0" r="7" />
            </g>
        </g>
    </svg>
</div>

</body>
</html>
